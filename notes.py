
#Python can do custom sorting with comparator
#Need a func that returns a negative number for less than,
#0 for equality or a positive number for greater than
#Then import cmp_to_key from functools and pass that
#to the key of the sort: x.sort(key=functools.cmp_to_key(comparator_func))
